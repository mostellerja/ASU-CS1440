/**
 * style.Java
 * 
 * @author Aaron Mosteller
 * @version 0.1
 */

/**
 * Test checkstyle in BlueJ.
 *
 * This javadoc header is required at the top of every class in
 * this file. Most of the time there will only be one class per
 * file, so you only need one of these. Also note that the version
 * tag below must be that last line on this javadoc comment and that
 * the author tag must immediately precede the version tag.
 *
 * @author Aaron Mosteller
 * @version 0.1
 */
public class Style
{
    /**
     * main – The program starts here in main.
     * Show some sample code to demonstrate common style
     * problems that are not really java problems.
     *
     * A javadoc comment describing the method is required at the
     * top of all methods in the class. If there are parameters a
     * "@param" tag is required describing each. If this method
     * returns a value a "@return" tag is required describing the
     * return value.
     *
     * @param args Command line arguments.
     */
    public static void main(String[] args)
    {
        int x = 1;
        int y = 2;
        System.out.println("x+y=" + (x + y));
    }
}
